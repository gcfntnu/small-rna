#-*- mode:snakemake -*-

ROP_INTERIM = join(INTERIM_DIR, 'smallrna', 'rop')
ROP_PROCESSED = join(PROCESSED_DIR, 'smallrna', 'rop')

extra_conf_fn = srcdir('rop.config')
if os.path.exists(extra_conf_fn):
    with open(extra_conf_fn) as fh:
        c  = yaml.load(fh) or {}
        # original config will override extra config on shared items
        # c=config, config=overwrite_config
        update_config(c, config)

include:
    'rop/kraken.rules'
