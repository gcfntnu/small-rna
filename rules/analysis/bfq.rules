BFQ_INTERIM = join(INTERIM_DIR, 'smallrna', 'bfq')


def bfq_counts(wildcards):
    if config['quant']['method'] == 'quickmirseq':
        return rules.quickmirseq_quant.output

rule bfq_preprocess_counts:

rule bfq_pca:

rule bfq_qc:

rule bfq_exprs:

rule bfq_aligned:

rule bfq_all:

